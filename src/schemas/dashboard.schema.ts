export interface IDashboardSchema {
  activeUser?: number;
  activeUserChange?: string;
  totalBuyers?: number;
  buyersChange?: string;
  totalSellers?: number;
  sellersChange?: string;
  totalEarning?: string;
  earningChange?: string;
}
